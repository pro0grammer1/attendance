import React from 'react';

const LendingPage = () => {
    return (
        <section className='print:[@page_{size:landscape}] w-[11.7in] m-auto h-[8.3in] border overflow-scroll print:overflow-hidden'>
            <table>
                <thead>
                    <tr>
                        <th>HEllo</th>
                    </tr>
                </thead>
            </table>
        </section>
    );
};

export default LendingPage;